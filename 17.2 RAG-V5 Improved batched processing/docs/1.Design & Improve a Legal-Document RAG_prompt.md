### 🧠 Prompt: Design & Improve a Legal-Document RAG Extraction Pipeline (Kenya Constitution + Acts)

You are an expert in **document processing**, **OCR**, **PDF extraction**, **legal-document analysis**, and **RAG pipeline optimization**, with a focus on **Kenyan legal documents** (e.g., the Constitution of Kenya, the Advocates Act, and related statutory instruments).

I have a RAG system that ingests legal PDFs (including scanned and digital-native documents), generates vector embeddings, and stores them in **pgvector**.  
The corpus includes documents such as:

- “TheConstitutionOfKenya.pdf”
- “Advocates Act.pdf”
- “Adjustment of Rates for Inflation (3).pdf”
- “Acts Published for the Implementation of the Constitution Corrigenda.pdf”

The system works, but we need to **upgrade the ingestion and extraction pipeline** to:

- Increase extraction accuracy
- Reduce noise (TOCs, headers/footers, etc.)
- Handle scanned documents reliably
- Improve table extraction and structure
- Enrich and standardize metadata
- Avoid duplicate or outdated versions
- Support high‑quality retrieval for downstream RAG over Kenyan law

Your task:  
Provide **step‑by‑step recommendations**, **architecture**, and **implementation-level guidance** to improve the pipeline.

---

## ✅ 1. Remove Noise During Extraction

We want to automatically remove or de‑prioritize:

- Table of contents
- Headers & footers
- Page numbers
- Repeated boilerplate elements
- Running titles, marginal notes, or banners (e.g., “The Constitution of Kenya, 2010”, “No. 6 of 2012”, etc.)

Explain:

1. **Detection strategies**
   - How to detect these reliably across different legal formats (constitutions, Acts, corrigenda, statutory instruments).
   - How to exploit layout cues (x/y coordinates, font size, repetition across pages) vs pure text heuristics.
   - How to handle edge cases like:
     - Side notes / marginal notes in Acts.
     - Bilingual or multi-column layouts.
2. **Where in the pipeline to remove them**
   - Pros/cons of:
     - Removing at **PDF/layout parsing stage** (pre-OCR for digital PDFs).
     - Removing **post-OCR** for scanned pages.
     - Filtering or re-weighting during **chunking**.
3. **Recommended tools / models**
   For each, explain the role:
   - `pdfplumber`, `PyMuPDF (fitz)`, `pypdf`.
   - `unstructured` (unstructured-io).
   - GROBID or S2ORC-style tools for structured section extraction.
   - Layout-aware models such as LayoutLMv3, DocLayNet-based detectors, or Donut/Smaller vision‑encoder text models.
   - Simple rule/heuristic layers (e.g., “same line repeated on >60% of pages” ⇒ header/footer).

Provide:

- A **concrete heuristic recipe** (pseudo-code or Python snippets) for filtering headers/footers and TOCs for a statute/Act of Parliament PDF.
- Advice on logging and QA (e.g., store both “raw text” and “cleaned text”, diffing tools to inspect what was removed).

---

## ✅ 2. Extract Scanned PDFs (OCR)

Some of these legal PDFs are scanned or mixed (image + text). Provide best practices to handle them:

1. **Scanned vs digital detection**
   - How to automatically detect if a page is:
     - Pure text (extractable via PDF text layer).
     - Pure image (requires OCR).
     - Hybrid/mixed.
   - Concrete approaches using:
     - PyMuPDF, `pdfminer.six`, or byte-level checks.
     - Heuristics like “no text elements above threshold ⇒ OCR”.
2. **Layout-aware OCR**
   - Recommend tools and how to configure them:
     - Tesseract (with LSTM), PaddleOCR, Google Document AI, Azure Form Recognizer, AWS Textract, etc.
   - How to:
     - Preserve reading order, columns, and section boundaries.
     - Maintain bounding boxes so we can later map text back to page regions.
3. **Tables, headers, and multi-column**
   - Techniques to:
     - Correctly detect and separate multi-column body text from side notes.
     - Preserve tables for table-specific processing.
4. **Minimizing OCR errors**
   - Language packs / training for:
     - English + Swahili (or other local languages if needed).
   - Lexicons or custom dictionaries for:
     - Kenyan legal terminology, case names, citations (e.g., “Cap. 16”, “No. 18 of 2014”).
   - Post-OCR normalization:
     - Spell-check + domain dictionary.
     - Simple regexes for common artifacts (broken section symbols, mis-read numbering).
   - Confidence thresholds and re-OCR strategies (e.g., re-run on low-confidence regions with different configs).

Provide:

- A **recommended OCR stack** and **minimal viable configuration**.
- Example pseudo-code or Python snippet showing:
  - How to route each page to text extraction vs OCR.
  - How to merge OCR text with layout metadata for downstream chunking.

---

## ✅ 3. Intelligent Table Extraction

Documents like “Adjustment of Rates for Inflation (3).pdf” may have tables of rates, thresholds, dates, etc.

Describe methods to **extract tables while preserving structure**:

1. **Table detection**
   - Visual-layout detection:
     - LayoutParser + Detectron2.
     - Document AI, Azure, or Textract table detection APIs.
   - Heuristic methods:
     - Line detection in PDF (ruling lines), cell border detection, whitespace grid patterns.
2. **Table recognition / parsing**
   - Tools:
     - Camelot, Tabula, PDFPlumber table extraction.
     - Deep models (DeepDeSRT, PubTables-1M style).
   - Comparison: accuracy vs engineering complexity.
3. **Structuring**
   - Converting tables to:
     - JSON with rows/columns + header detection.
     - Markdown tables for embedding.
     - Hybrid representation: text summary + JSON blob.
   - Guidance on:
     - When to **keep tables intact** and represent as a single “table chunk”.
     - When to **flatten** into “row per fact” or long text (e.g., to support question-answering over tax bands or inflation adjustments).

Provide:

- A **table extraction and representation strategy** suitable for legal/rates tables.
- Example schemas, e.g.:

  ```json
  {
    "doc_id": "adjustment_rates_2018",
    "page": 12,
    "table_id": "t1",
    "title": "Adjustment of Rates for Inflation – 2018",
    "headers": ["Category", "Old Rate", "New Rate", "Effective Date"],
    "rows": [
      ["Band A", "5%", "7%", "2018-07-01"],
      ...
    ]
  }
  ```

- Advice on embedding:
  - Separate **embedding space** for tables vs normal paragraphs, or same?
  - Special prompts or system tags in text representation (e.g., `TABLE:` prefix).

---

## ✅ 4. Detect Newer Versions & Avoid Duplicate Ingestion

We regularly ingest:

- New amendments to Acts
- Corrigenda (e.g., “Acts Published for the Implementation of the Constitution Corrigenda.pdf”)
- Slightly modified PDFs (e.g., updated cover page or publisher watermark)

Explain how to detect and manage:

1. **Near-duplicate documents**
   - Techniques:
     - Raw file hashes (MD5/SHA256).
     - **Content hashes** (hash of normalized text).
     - Shingling + MinHash/SimHash for near-dup detection.
     - Embedding-based similarity across whole-document vectors.
2. **Revised versions**
   - Strategies for:
     - Detecting that a file is a **new version** of the same Act (same title + jurisdiction + largely overlapping content).
     - Comparing normalized section lists and checking differences.
   - Handling Corrigenda:
     - Detect that a Corrigendum modifies a base document.
     - Represent the relationship in metadata (base_doc_id, amendment_date, changed_sections).
3. **Metadata and canonical versioning**
   - How to maintain a **canonical document record**:
     - `doc_id` (stable).
     - `version_id` (per file/version).
     - `status` (current / superseded / draft).
   - When to:
     - Skip ingestion.
     - Update embeddings in place.
     - Store historical versions but mark them as low priority in retrieval.

Provide:

- A concrete **versioning and dedup design**:
  - Recommended fields: `fingerprint`, `text_hash`, `shingle_hash`, `parent_doc_id`, `version_number`, etc.
- A step-by-step process:
  - “On ingest, compute X hashes, compare against Y, decide to ingest/skip/update”.
- Example queries in SQL or pseudo-SQL using pgvector for near-dup search.

---

## ✅ 5. Extract and Recommend Metadata

The system should extract rich metadata for **legal PDFs**, including the Kenyan context.

### 5.1 Recommended metadata fields

Provide a table of recommended fields with:

- **Field Name**
- **Type** (string, date, enum, list, boolean, numeric)
- **Extraction method** (regex, layout-based, model-based, manual)
- **How it helps retrieval/filtering**

Include at least:

- `doc_id`
- `version_id`
- **Title**
- **Short title / popular name** (e.g., “Constitution of Kenya 2010”)
- **Citation / Cap / Act number** (e.g., “Cap. 16”, “No. 18 of 2014”)
- **Author / Issuing body** (e.g., Parliament, Judiciary, Commission)
- **Source / URL**
- **Publisher**
- **Page count**
- **Document date(s)**:
  - Enactment/assent date
  - Commencement date
  - Publication date
- **Version info**:
  - “Revised edition 2012”, “2010”, etc.
- **Jurisdiction** (e.g., Kenya; optionally county for devolved legislation)
- **Document type** (Constitution, Act, Regulation, Notice, Guideline, Corrigendum, Subsidiary legislation, etc.)
- **Language(s)** (English, Swahili, bilingual)
- **Sections & hierarchy**:
  - Part / Chapter / Section / Article numbers & titles (e.g., “Article 43 – Economic and social rights”).
- **Entities**:
  - Courts, Commissions, Ministries, Counties, Legal entities.
- **Subject-matter tags**:
  - e.g., “Human rights”, “Tax”, “Advocates & Legal Practice”, “Procedure”.
- **OCR quality / parser confidence**:
  - Page-level and document-level scores.
- **Derived fields** (optional):
  - `is_scanned`, `has_tables`, `has_amendments`, `superseded_by`, etc.

### 5.2 Extraction methods

For each major field class, describe:

- Heuristic extraction (e.g., regex patterns for “ACT NO.”, “ENACTED by the Parliament of Kenya”, “Commencement: …”).
- Layout cues (title usually largest font, centered on page 1).
- Model-based extraction:
  - Using sequence labeling (NER) or prompt-based extraction with an LLM.
  - Using document-understanding APIs (Google Doc AI / Azure / AWS).

Provide:

- Example **regexes or parsing logic** for:
  - Kenyan Act citations.
  - Section/Article headings (`ARTICLE 43—`, `Section 12.`, etc.).
- Suggestions for:
  - **Fallback strategies** when automatic extraction is low-confidence.
  - How to store metadata in a relational schema aligned with pgvector docs.

---

## 📌 Deliverables

Your response must include:

1. **Improved extraction pipeline architecture**

   - Present a step-by-step architecture, e.g.:

     1. Ingestion & file hashing
     2. Digital vs scanned page classification
     3. Layout parsing & OCR
     4. Noise removal & normalisation
     5. Structure detection (sections, headings, tables)
     6. Metadata extraction
     7. Chunking strategy
     8. Embedding generation & pgvector upsert
     9. Versioning & deduplication
     10. QA, logging, and monitoring

2. **Recommended tools & models**

   - Name specific tools/models for each stage, including at least one open-source and one managed/cloud option where applicable.

3. **Sample code snippets**

   - Use concise Python where helpful (pseudocode OK but should be close to runnable).
   - Focus snippets on:
     - Page routing (OCR vs direct extraction).
     - Header/footer detection.
     - Versioning/dedup checks.
     - Basic table extraction to JSON.

4. **Data validation & QA tips**

   - How to:
     - Spot-check ingestion quality (e.g., random pages).
     - Track OCR accuracy over time.
     - Validate section-level completeness (e.g., all Articles present).
     - Build simple dashboards or metrics.

5. **Optional: RAG-specific guidance**
   - Chunking:
     - Section/article-aware chunking.
     - How to use legal hierarchy in chunk IDs (e.g., `constitution_2010:art.43(1-3)`).
     - Handling long tables vs narrative text.
   - Embeddings:
     - Recommended models for legal English (and Swahili, if relevant).
     - Whether to maintain separate embeddings for:
       - Main text
       - Tables
       - Metadata / headings.
   - Prompting:
     - System prompts that instruct the model to:
       - Cite specific Articles/Sections.
       - Prefer the most recent version.
       - Surface jurisdiction and date in answers.

---

## 🎯 Goal

Produce a **complete, production-ready plan** for upgrading a legal-document RAG extraction pipeline over Kenyan legal PDFs.  
The plan should emphasize:

- High-fidelity text and table extraction
- Robust OCR handling for scanned documents
- Noise reduction (TOCs, headers, footers, boilerplate)
- Sophisticated deduplication and version control
- Rich, reliable metadata for powerful retrieval and filtering
- RAG-aware chunking and embedding strategies suitable for constitutional and statutory queries.
